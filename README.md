# An Empty Pico Project

This repository contains an empty 'initial' project for Pi Pico called project1 which can be renamed and used as a basis to add code to create a real project.

You'll need to set the actual desired project name in CMakeLists.txt.

To build from the command line using Windows, see the BUILD_INSTRUCTIONS_WINDOWS_POWERSHELL.txt file

